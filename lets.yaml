shell: bash

env: {}
eval_env: {}

commands:
  create_volumes:
    cmd: mkdir -p .volumes

  infra:
    depends:
      - create_volumes
    cmd:
      docker-compose up

  server:
    cmd: cargo run

  example-client:
    cmd: cargo run --example client